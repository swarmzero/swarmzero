model:
  max_iterations: 10
  model: "gpt-3.5-turbo"
  enable_multi_modal: true

environment:
  type: "dev"

timeout:
  llm: 30

log:
  level: "INFO"

test_agent:
  max_iterations: 10
  model: "gpt-4"
  

logging:
  log_file_path: "swarmzero.log"
  log_level: "INFO"
